/*
 *  JS file for about.html-only code
**/

$(document).ready(function() { // Cargar HTML

    if(window.location.href.indexOf('about') > -1) { // Comprobar que estamos en about.html

        // Tabs section
        $('#tabs').accordion()
        
    } // if - check about

})